package turismoEnLaTierraMedia;

import java.util.ArrayList;
import java.util.List;

public class GenerarSugerencias {
	//Para revisar
	
	/*
	private List<Producto> promociones;

	public GenerarSugerencias(List<Producto> promociones) {
		this.promociones = promociones;
	}

	public List<Producto> getSugerencias(Usuario usuario) {
		List<Producto> sugerencias = new ArrayList<Producto>();
		for (Producto sugerencia : this.promociones) {
			if (usuario.precioCorrecto(sugerencia.getCosto()) 
				&& usuario.tengoTiempo(sugerencia.getDuracion())
				&& usuario.estanBienTiposAtraccion(sugerencia.getListaDeTiposAtraccion())) 
				{
					sugerencias.add(sugerencia);
				}
		}
		return sugerencias;
	}
	*/
}
